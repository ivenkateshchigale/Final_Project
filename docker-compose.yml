services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: voice_banking_app
    ports:
      - "80:8001"
      - "8001:8001"
    env_file:
      - .env
    restart: unless-stopped
